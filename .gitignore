# Dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native (generated by `expo prebuild` — CNG keeps these out of VCS)
/ios/
/android/

# Metro / caches
.metro-health-check*
*.tsbuildinfo

# Testing
coverage/

# Environment & secrets — NEVER commit encryption keys or PHI
.env
.env.*
*.p12
*.keystore
*.mobileprovision

# OS / editor
.DS_Store
*.log
